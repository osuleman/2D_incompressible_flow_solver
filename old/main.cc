/*      FCFD Semester Project  
	Part 1  
	Omar Suleman 
	13/03/13  
*/

#include <iostream>
#include <cmath> 
using namespace std;

int main()
{


/* parameters */
	int it,nx,ny,nz,nt
	double u(nx,ny,nz)
	double L,W,D
	double pi,dt,dx,dy,t,amplitude,t_end,eps


/* Define Grid Parameters */
	int nx = 50;
	int ny = 50;
	int nz = 1;

/* Geometry */
	double L = 50;
	double W = 50;
	double D = 1;

/* Definitions */
	double pi = 4*atan(1.);
	double dx = L/nx;
	double dy = W/ny;
	double dz = D/nz;





return 0;
}

