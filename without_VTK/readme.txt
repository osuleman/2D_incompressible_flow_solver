go into sources and edit Main.cpp accordingly                  "cd sources" edit main as desired and set results directory
go back one directory, then into Build                         "cd ../Build"
if you're not using gcc, specify compiler (CC)in makefile      "CC = *"
run makefile clean                                             "make clean"  
run make                                                       "make"
run Main.exe                                                   "./Main.exe"
